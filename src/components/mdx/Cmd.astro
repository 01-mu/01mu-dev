---
const { class: className } = Astro.props;
---

<pre class:list={["mdx-cmd", className]}>
  <code><slot /></code>
</pre>

<style>
  .mdx-cmd {
    margin: 0.75rem 0;
    padding: 0.5rem 0.75rem;
    border-radius: 6px;
    background: rgba(28, 42, 51, 0.06);
    color: var(--ink);
    font-size: 0.9rem;
    line-height: 1.35;
    overflow-x: auto;
  }

  .mdx-cmd code {
    font-family: "JetBrains Mono", "SFMono-Regular", "Consolas", "Liberation Mono",
      monospace;
    font-size: 0.95em;
  }
</style>
