---
const { class: className } = Astro.props;
---

<section class:list={["mdx-context", className]}>
  <slot />
</section>

<style>
  .mdx-context {
    margin: 1.25rem 0;
    padding: 0.75rem 1rem;
    border-radius: 6px;
    border: 1px dashed rgba(28, 42, 51, 0.35);
    background: rgba(28, 42, 51, 0.05);
    color: var(--muted);
    font-size: 0.95rem;
  }
</style>
