---
const { class: className } = Astro.props;
---

<aside class:list={["mdx-note", className]}>
  <slot />
</aside>

<style>
  .mdx-note {
    margin: 1.25rem 0;
    padding: 0.75rem 1rem;
    border-radius: 6px;
    background: rgba(28, 42, 51, 0.06);
    color: var(--muted);
    font-size: 0.95rem;
  }
</style>
